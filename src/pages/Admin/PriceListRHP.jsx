// Reserved for RHP Price List for embedding
export default function PriceListRHP() {

    return (
        <div className="w-full h-full">
            {/** Main contents goes inside here please, I put an iframe as place holder */}
            <h3>Now we are in a separated component ~</h3>
            <p>And this is just a place holder leaved for Joshua to complete (as demanded)</p>
        </div>
    );
}
